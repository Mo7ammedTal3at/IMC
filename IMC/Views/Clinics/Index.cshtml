@model IEnumerable<IMC.Domain.DomainModels.Clinic>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>

<div class="LI-profile-badge" data-version="v1" data-size="medium" data-locale="ar_AE" data-type="vertical" data-theme="dark" data-vanity="mohamed-megahed-a2a02012b"><a class="LI-simple-link" href='https://eg.linkedin.com/in/mohamed-megahed-a2a02012b?trk=profile-badge'>Mohamed Megahed</a></div>

<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.FlowerNumber)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.FlowerNumber)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Id }) |
            @Html.ActionLink("Details", "Details", new { id=item.Id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Id })
        </td>
    </tr>
}

</table>
